<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apple 2024 Products</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Apple 2024 Products</h1>
        <ul>
            <% items.forEach(item => { %>
                <li>
                    <a href="/detail?id=<%= item.id %>"><%= item.name %></a>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
